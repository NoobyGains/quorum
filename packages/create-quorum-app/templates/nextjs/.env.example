# Advisory spend cap for Quorum campaign runs (USD). Soft-warn at target,
# hard-stop at 2x. Leave unset to disable.
QUORUM_CAMPAIGN_BUDGET_USD=5

# Base URL used by server components to reach the app's own API routes.
# Override in production.
NEXT_PUBLIC_BASE_URL=http://localhost:3000
